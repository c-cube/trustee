(rule
  (alias runtest)
  (action (diff? prelude_hol.parsed.expected prelude_hol.parsed)))

(rule
  (targets prelude_hol.parsed)
  (deps prelude_hol.trustee)
  (action
    (with-stdout-to
      %{targets}
      (run ../src/tools/trustee_bin.exe cat %{deps}))))
